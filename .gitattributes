# 统一换行符策略（团队规范）
# 所有文本文件在仓库内统一以 LF 存储，检出到工作区时也保持 LF，
# 避免 Windows(CRLF) / Unix(LF) 混用导致的无意义 diff 与 CI 行尾检查失败。
* text=auto eol=lf

# 二进制资源保持原样，禁止换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
