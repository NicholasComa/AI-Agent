"""DevAssistantAgent 工具子包。

Week 3 计划提供三个工具：

* ``calculator``           — ``ast`` 安全求值
* ``read_text_file``       — ``Path.resolve() + is_relative_to(TRAIN_DIR)`` 防穿越
* ``check_commit_message`` — 团队 commit 模板正则校验
"""
